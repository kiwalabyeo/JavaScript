let interest = 1.8;
// for the constant to not change we change let to const

const interesting = 1.8;
// let interesting = 2; this would cause an error because 'interesting' is a constant and we cannot reassign it
console.log(interesting);